# 问题二改进方案 · 独立复核报告

- 复核人：reviewer（独立复核员，未参与本改进的实现）
- 复核对象：`问题二_改进求解/`（README.md、对比报告.md、q2_solver4.py、parallel4.py、parallel4b.py、q2_verify.py、joint_recon.py、pool_opt.py 及其余辅助脚本）；结果目录 `results/问题二_改进方案/`（4 个口径方案 + Pareto前沿_问题二.csv + 完成时间上限实验.csv）；另核对 `results/问题二_改进方案/问题二_改进方案_结果说明.md`
- 原题：`D:\git\math_modeling\proj-d-uav-flood\题目原文_含公式.txt`
- 复现环境：Python 3.12.8 / numpy 2.0.1 / scipy 1.15.2 / joblib 1.4.2（与 README 声明一致）；ortools 未安装（与 README 局限 4 一致）
- 复核方式：**不复用求解器中间量**。直接从 `运输无人机数据.xlsx`、`物资需求与配送时限.xlsx`、`有向航段几何参数.csv` 读取原始参数，按附录 2 公式自行实现能耗/时间/交付时刻，不使用 `问题二_调度核心.segment`，也不导入 `load_data`。
- 本轮**未修改任何方案数值、结果文件或已有代码**；所有临时计算脚本均写在 `%TEMP%` 下。

---

## 1. 断言 A–F 的判定与复算证据

### A. 基准全精度指标与相对原主方案的改善 —— **成立**

独立复算（自读 xlsx + 几何表，自实现公式）：

| 方案 | 架次数 | 完成时间 / s | 总能耗 / kWh | hard / s | 加权延误 |
|---|---:|---:|---:|---:|---:|
| 新时间优先（主方案） | 24 | **5903.038154612774** | **65.89095992013699** | 0.0 | 0.0 |
| 原主方案（`问题二_参考口径/主方案_完整方案.json`） | 25 | 6410.480649299858 | 69.96489527172167 | 0.0 | 0.0 |

- 差值复算：`ΔT = 507.44249468708404 s = 7.915826%`；`ΔE = 4.07393535158468 kWh = 5.822828%`；`Δ架次 = 1`。
  文档写作 `507.442495 s（7.9158%）`、`4.073935 kWh（5.8228%）`、少 1 架次 —— 一致（四舍五入层面无误）。
- 最低返航 SOC：原主方案 `0.2186581 → 21.87%`，新主方案 `0.2013730 → 20.137%`，收紧 `−1.729 pp`；文档 `21.87% → 20.137%，−1.73 pp` —— 成立。
- README 第 1 节给出的 4 个 SHA-256（两个 xlsx、几何表、时间优先基准 JSON）逐字节复算 **全部一致**。
- 独立命令：`python code/问题二_改进求解\q2_verify.py` → `PASS 4 / 4`（退出码 0）。
### B. 四个口径方案的题面硬约束 —— **成立**

1. `python code/问题二_改进求解\q2_verify.py` 实跑输出（原文）：

```
方案_均衡_完整方案.json             PASS  n=20 T=  7299.970 E= 61.7097
方案_时间优先(主方案)_完整方案.json   PASS  n=24 T=  5903.038 E= 65.8910
方案_架次优先_完整方案.json          PASS  n=18 T=  8171.565 E= 60.4418
方案_能耗优先_完整方案.json          PASS  n=18 T=  8237.565 E= 59.4658
PASS 4 / 4
```

2. 我另写独立脚本（不使用 `问题二_调度核心.segment`，也不使用其 `load_data`）对**四个方案全部逐架次重算**，结论 `ERRORS: none`：

| 方案 | n | makespan / s（复算） | energy / kWh（复算） | hard | tardy | 最低 SOC | 每架次最大站数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 时间优先（主方案） | 24 | 5903.038154612774 | 65.89095992013699 | 0 | 0 | 0.201373 | 2 |
| 均衡 | 20 | 7299.970162268759 | 61.709747853750436 | 0 | 0 | 0.201373 | 2 |
| 能耗优先 | 18 | 8237.565447127903 | 59.4658115950177 | 0 | 0 | 0.205652 | 2 |
| 架次优先 | 18 | 8171.565447127903 | 60.44180693383757 | 0 | 0 | 0.205652 | 2 |
逐项核验结果（四个方案均通过）：
- **80 箱唯一覆盖**：`sorted(箱号) == sorted(货箱清单)`，且无重复；
- **逐架次质量/体积**：均不超过机型上限，且与 JSON 的 `mass_kg` / `volume_m3` 字段一致；
- **逐段剩余载荷**：按「交付后扣减站内箱重」递推，末段返航载荷为 0；由此重算的航段能耗与架次能耗逐条相符；
- **能耗**：逐架次复算与 `energy_kwh` 差 < 1e-6，总和与 `metrics.energy_kwh` 差 < 1e-9；
- **返航 SOC ≥ 20%**：`1 − E / E_use ≥ ρ`，四个方案最低 20.1373%，且与 `return_soc` 字段一致；
- **逐箱交付时刻**：80 箱全部按 `起飞 + 准备 + 装载 + Σ(航段 + 交接)` 复算，与 `deliveries` 逐箱相符（< 1e-6）；
- **硬时限与期望时间**：31 个硬截止箱 `hard = 0.0`，加权延误 `tardy = 0.0`（医疗物资期望时间按 hard 处理，与参考口径一致）；
- **无人机/电池占用不重叠**：无人机区间 `[start, return]`、电池区间 `[start, charge_end]` 逐资源排序后无交叠；机型-电池分组匹配；用量未超库存（A 6 / B 4 / C 4 组电池，A 4 / B 2 / C 2 架）；
- **额外独立检查（`q2_verify.py` 未覆盖）**：① 每箱所属服务区与所访问站点一致（`BOX[箱].area == stop.area`）；② 同一架次内站点不重复；③ 逐航段 / 逐架次 / 逐箱交付三张 CSV 与对应 JSON 在文档声明的 6 位（时刻、能耗）与 3 位（航段时间、距离）舍入下**全部一致**（主方案 50 / 24 / 80 行，其余 42 / 20 / 80、39 / 18 / 80、39 / 18 / 80）。

3. 附带核验（同属结果目录、非 A/B 断言但影响可采信度）：
- `完成时间上限实验.csv` 6 行全部用 `cap_best_*.json` 的 specs 独立重算，`T / E / n / A-B-C` **逐行一致**（5903→5903.038/65.8910/24；6000→5903.038；6600=7200→6132.337/64.6745/23；8000→7846.229/60.0241/19；9000→8237.565/59.4658/18）；
- `Pareto前沿_问题二.csv` 16 行两两互不支配（0 对被支配）。
### C. 最优性/瓶颈声明（C 型 2 架机的 6 架次二划分）—— **成立**

- 从主方案 JSON 直接取 C 型 6 架次（耗时 = 返回 − 开始，全精度）：
  `T04 1561.553867937`、`T05 1494.169077325`、`T07 2107.812384806`、`T12 2436.091349560`、`T14 2301.056692482`、`T15 1877.190524525`；合计 `11777.873896634283 s`（/2 = 5888.936948317141）。
- 自行枚举全部 `2^6 = 64` 种二划分：**最优最大子集和 = 5903.038154612774 s**，达到者为 `{1494.169077325, 2107.812384806, 2301.056692482}`；存在两个子集都小于 5903.038 的划分数 = **0**。文档给出的 6 个三位小数（2436.091、2301.057、2107.812、1877.191、1561.554、1494.169）与该全精度值一致。
- 该值与本方案实际 `makespan_s` **完全相等**，即 C 型实际排程已取到二划分下界；另核 A 型 11 架次总时长 `19505.489862642447`（/4 = 4876.372）、B 型 7 架次 `11546.13338383198`（/2 = 5773.067），与 README §5 数字一致。
- 我进一步做了更强的检查：对固定架次集合做「纯重排」下界（A 型 4 机最优划分 5105.131、B 型 2 机最优划分 5812.714、C 型 2 机最优划分 5903.038），其最大值 = `5903.038154612774` = 实际 makespan ⇒ **在固定组批/路线/机型下，任何重排都无法低于 5903.038155 s**（比文档声明更强，且与之一致）。
- 文档**没有**把它写成全局下界：README §5 末句「该值只是固定任务组合下的最优，不是原问题全局下界」、对比报告 §5.5「没有全局最优性证明」—— 表述恰当。
- 补充观察（不构成问题）：`均衡`、`能耗优先` 的方案 makespan 高于「忽略充电的二划分下界」（6871.840 vs 7299.970；8179.788 vs 8237.565），但用与解码器同口径的**全排列 + 可行性过滤**枚举可知其 C 型顺序已是最优、且 C 型决定总 makespan，故**没有任何重排能改善这四个方案的公开指标**；上述差值是「电池充放电约束 + 贪心调度器」造成的调度器能力问题，不影响数值正确性。
### D. 搜索空间声明（q2_solver4.py 的两处改动 / 4 站是搜索截断）—— **成立（含一处措辞瑕疵）**

- `difflib.unified_diff(问题二_求解.py, q2_solver4.py)` 的全部差异只有 4 处文本：
  1. 第 1 行新增 UTF-8 BOM（`\ufeff`）；
  2. `if len(areas) > 3:` → `if len(areas) > 4:`（合并架次算子的站数上限）；
  3. `if len(spec["stops"]) < 3 and …` → `< 4`（移除重插 → new_stop 的站数上限）；
  4. 文件末尾多一个空行。
  **唯一的两处语义改动就是这两个站点上限**（`>3→>4`、`<3→<4`），与文档一致；SHA-256：原 `dbbb6575…ae3652`，新 `d5d70f2d…c18ac9`。
- 题面核对：`题目原文_含公式.txt` 问题二原文为「每个运输架次可访问一个或多个服务区，并在完成物资交付后返回 O01」，**题面未限制每架次站数**。
- 文档确实把 4 站声明为搜索截断而非题面约束：README §1/§2「放开到 ≤4 站」、局限 1「4 站是**搜索截断**，不是题面约束；未证明 >4 站无更优解」；对比报告 §5.5 同义。**成立**。
- 但需注意（见问题清单 H-2）：仓库内**全部**已产出方案（4 个发布方案、6 个 `cap_best_*.json`、10 个 `中间过程/*完整方案.json`、参考口径 12 个方案）每架次站数的最大值都只有 **2**，即第 3、4 站容量在所有公开结果中从未生效。

### E. 负结果记录（joint_recon.py / pool_opt.py）—— **成立（记录诚实），细节不可复现**

- `joint_recon.py`（为不污染仓库，复制到 `%TEMP%` 运行）：固定 A/B、重构 C 的 36 箱、最小化各机型工作量上界 Z。实跑：候选 497 条 → HiGHS 最优 `Z = 5773.067`（与 README 相同）→ 全精度解码 `n=26 T=6741.200 E=68.093 hard=3518.4 tardy=117600.2`，即**不可行，未产出改进**；脚本本身不把该解写入结果目录（仓库未保存 `joint_recon.json`）。
- `pool_opt.py`（同样在 `%TEMP%` 运行）：候选池 + 联合调度 MILP + no-good 割。实跑：池 24 条 → 第 1 轮 MILP 最优 `Z = 5888.937`（恰好等于发布方案自身的工作量上界）→ 未找到可行且更优的排程 → 加割后第 2 轮 MILP 不可行 → 脚本输出 `BEST None`，即**未产出可行改进**。
- README §6 与对比报告 §2（V3 行）、§5.6 均把它记为「未获得可行改进 / 负结果 / 不作为数值改善」，**没有**在未找到更优解的情况下把方法升级表述成数值改善。判定：**成立**。
- 但文档细节：README §6 写 joint_recon「hard=4859.4 s 违约」，我在当前仓库状态下只复现出 `hard=3518.4 s`（定性的解码不可行成立，数值不一致）；pool_opt 的「池 1564 条 / 10 轮内 Z 降到 5542.046」与「完成时间上限搜索的 404 个候选」在当前仓库状态下均**无法复现**（原因见 M-3：`runs/*.json` 不存在）。
### F. 夸大/无法复现表述 —— **不成立（确认存在夸大与错误表述，见问题清单）**

逐条证据见第 2 节 H-1、H-2、M-1、M-2/M-3、L-1 至 L-5。摘其要：
- `results/问题二_改进方案/问题二_改进方案_结果说明.md:3` 写「**逐机型精确调度**」，既与 README 局限 4「不是精确调度」自相矛盾，也被产物证伪（见 M-1）；
- 对比报告 §4 的原主方案「硬时限最小余量 216.953（S004-MED-01）」**是错的**（见 H-1）；
- 对比报告 §2 结论「主要改善来自放开 4 站 + 多起点长时并行搜索」没有单变量消融支撑（见 H-2）。

---

## 2. 问题清单（按严重度分级）

> 说明：**本轮未发现任何方案数值、可行性或指标复算错误**（A 至 E 全部成立），因此 blocker = 0。以下均为「声明与证据不符 / 可复现性」类问题，其中 H 级影响对外声明的可信度。

### H-1（high）对比报告 §4 的原主方案硬时限最小余量数字错误

- 位置：`问题二_改进求解/对比报告.md:40`（表格行）、第 44 行叙述。
- 事实：对比报告写「原发布主方案：硬时限最小余量 **216.953 s（S004-MED-01）**」。用原始数据独立复算原主方案（`results/问题二_参考口径/主方案_完整方案.json`）全部 31 个硬截止箱的余量，最小者是 **`S012-WAT-01` 的 204.169936 s**（交付 3395.830064 s / 截止 3600 s）；`S004-MED-01` 的实际余量是 **1621.889984 s**（相差一个数量级）。参考口径自己的说明文档（`results/问题二_参考口径/问题二结果说明.md:29`）也写「最小余量为 `S012-WAT-01` 的 **204.170 秒**」，与对比报告互相矛盾。
- 影响：第 44 行「以更小的硬时限最小余量（216.953→144.170 s）换取改善」的幅度被夸大 12.78 s（真实为 204.170→144.170，即 −59.999 s）。方向正确，数字错误。
- 最小修复：该行改为 `204.170（S012-WAT-01）`，第 44 行改为 `204.170→144.170 s`；并核对该表的生成来源（疑为另一版本方案或人工转录）。

### H-2（high）「改善来自放开 4 站」的归因无证据支撑

- 位置：`问题二_改进求解/对比报告.md:19-23`（V0/V1/V2 消融表与结论）、`README.md:4`。
- 事实：① V1→V2 同时改变了两个因素（站点上限 3→4 **和** 换成多起点长时并行搜索），不构成单变量消融；② 仓库内全部已产出方案（4 个发布方案、6 个 `cap_best_*.json`、10 个中间过程方案、参考口径 12 个方案）**每架次最多 2 站**，而参考口径的 3 站方案本身也都 ≤2 站 —— 第 3、4 站容量在任何公开结果里从未生效；③ `runs/*.json` 缺失，无法证明搜索过程中曾借助 3/4 站中间解跳出局部最优。
- 影响：结论「**主要改善来自放开 4 站 + 多起点长时并行搜索**」不可复现、当前证据不支持；真正可验证的只是「候选解落在 ≤2 站解空间内、由更长/多起点搜索得到」。
- 最小修复：要么补做同预算、只改站点上限（3 vs 4）的对照实验；要么把表述改为「本轮未做单变量消融；最终方案最多 2 站，第 3/4 站容量未生效，改善更可能来自长时/多起点搜索」，并在表中把 V2 拆成可分辨的两行。
### M-1（medium）「逐机型精确调度」与方法事实不符

- 位置：`results/问题二_改进方案/问题二_改进方案_结果说明.md:3`；相关：`问题二_改进求解/README.md:26`（把 `feas_sched.py` 列为方法组成）、`README.md:90`。
- 事实：① 发布四口径方案的脚本 `finalize4.py` 只调用 `问题二_调度核心.decode`，**从未 import 或调用 `feas_sched.py`**（全目录 grep：只有 `finalize4.py` / `cap_summary.py` / `front_final.py` / `export_caps.py` / `validate_export.py` 直接使用 `decode`）；② 用与 `feas_sched.py` 同口径的**全排列 + 硬时限/延误可行性过滤**枚举各机型全部顺序，`架次优先`、`均衡`、`能耗优先` 的 B 型都存在**可行且更短**的顺序（分别可缩短 627.8 s、92.1 s、557.6 s），而 `feas_sched.py` 对 k ≤ 8 走全排列（B 型 k=7/8），若真被应用必然采用它们；③ 只有主方案的 B/C 已是顺序最优。
- 影响：**公开指标未变**（四个方案的 makespan 均由 C 型决定，而 C 型顺序已被我穷举证明最优），因此这不是数值错误；但「精确调度/逐机型调度优化」的方法描述与产物不符，且三份方案的 B 型排程并非逐机型最优。
- 最小修复：把「逐机型精确调度」改为「单机贪心解码 + 可选的逐机型重排脚本 `feas_sched.py`（未应用于发布方案，属启发式）」；README §2 注明 `feas_sched.py` 未被 `finalize4.py` 调用。

### M-2（medium）README §3 的复现命令在当前仓库直接报错

- 位置：`问题二_改进求解/README.md:34-38` 与 `parallel4.py:55`（`parallel4b.py` 同类）。
- 事实：实跑 `python code/问题二_改进求解\parallel4.py 2 1 1 <out>` 立即抛
  `FileNotFoundError: results\问题二_改进方案\方案_完成时间优先(主方案)_完整方案.json`（该文件只存在于 `中间过程/`，发布目录里已改名为 `方案_时间优先(主方案)_完整方案.json`）。
- 影响：README 列出的「复现命令」无法原样执行；且真正把 `runs/*.json` 变成发布文件的 `finalize4.py`（以及 `front_final.py`、`cap_summary.py`、`export_caps.py`）**完全没有出现在复现步骤里**。
- 最小修复：修正两个 parallel 脚本的入参文件名（或在 README 中给出改名后的路径），并在 §3 补上 `finalize4.py → front_final.py → cap_summary.py → export_caps.py → q2_verify.py` 的完整链路。

### M-3（medium）`runs/*.json` 缺失，导致多处文档数字不可复现

- 位置：`问题二_改进求解/runs/`（空目录）、`README.md:41,71-77`、`对比报告.md:19`。
- 事实：`runs/` 为空，`par4.json / par4b.json / prio.json / pareto.json` 均不存在（未纳入版本库）。后果：
  - `pool_opt.py` 的候选池由文档的 1564 条退化为 **24 条**，「10 轮内 Z 降到 5542.046」无法复现（我实跑第 2 轮即不可行、`BEST None`）；
  - `joint_recon.py` 的池由文档规模退化为 497 条，README 的 `hard=4859.4 s` 无法复现（我得到 3518.4 s，定性结论相同）；
  - `cap_summary.py` 的 404 个候选、对比报告 V1 行「6027.234 / 69.089 / 25（约 280 次运行）」在仓库中无任何可追溯产物；
  - `Pareto前沿_问题二.csv` 16 行中，第 1、2、4、11、14、15、16 行能与已存 JSON 对上（发布方案 / 中间过程 / `cap_best_*`），其余 9 行（第 3、5、6、7、8、9、10、12、13 行）在仓库内没有对应的 specs 文件，无法独立重算。
- 影响：数值结论本身已被我对 4 个方案 + cap_best 的独立复算证实；但「可复现」这一自述只在「环境与工作脚本 + 最终文件」层面成立，生成链无法重放。
- 最小修复：把 `runs/*.json`（或 16 个非支配点的 specs）入库，或把「复现」表述降级为「结果文件可独立验证，搜索过程不可重放」。
### L-1（low）`q2_verify.py` 文档字符串与实际行为不符

- 位置：`问题二_改进求解/q2_verify.py:4`：「…并检查导出 CSV 的六位小数舍入」。代码只读 JSON 与几何表，**从未打开任何导出 CSV**（我已另行独立核验三张 CSV 与 JSON 一致，故结论未受影响）。
- 最小修复：删除该句，或补上 CSV 舍入校验。

### L-2（low）「仅两处改动」的措辞与 BOM

- 位置：`问题二_改进求解/README.md:23-24`。
- 事实：实际 diff 为 4 处（新增 BOM、两处站点上限、末尾空行）。BOM 不影响执行（`parallel4.py` 已成功 import 该模块）。
- 最小修复：改为「唯一的两处**语义**改动」；顺手去掉 `q2_solver4.py` 的 BOM。

### L-3（low）遗留脚本 `validate_export.py` 会写入发布结果目录

- 位置：`问题二_改进求解/validate_export.py`。
- 事实：它读取不存在的 `best_feasible.json`，并把 `改进方案_逐架次.csv / 改进方案_逐箱交付.csv / 改进方案_完整方案.json` 写入 `results/问题二_改进方案/`；README 未引用它。为免写入发布目录，本轮未执行该脚本。
- 最小修复：移入 `legacy/` 或删除，或在写文件前加确认。

### L-4（low）P-01/P-02 的口径指引指向不明确

- 位置：`问题二_改进求解/README.md:88`、`对比报告.md:44`（写作「README 的 P-01/P-02」）。
- 事实：P-01（作业高度基准）/ P-02（DEM 像元语义）实际记录在 `results/建模算法审计/12_独立核验原始输出.json` 的 `元信息.口径`，并非本 README 定义；仓库根 README 指向的 `results/建模算法审计/13_复核裁决.md` 在本仓库中不存在。
- 补充（对「SOC 较紧」风险的定量）：该审计记录 P-01 口径切换的影响为 `ΔE +0.0017% / ΔT −0.0168% / SOC +0.0043 pp`，比主方案 0.137 pp 的 SOC 余量小约 30 倍 —— README 局限 3 的谨慎提示成立，但风险有界。
- 最小修复：把引用改为具体文件路径与条目号。

### L-5（low）joint_recon 的 Z 下界未说明

- 位置：`问题二_改进求解/README.md:70-72`。
- 事实：文中 `Z = 5773.067` 恰好等于**固定不变的 B 型**每机工作量 `11546.133/2`，即该 MILP 的目标被固定部分锁定，无法降到 5773.067 以下；README 只把失败归因于「MILP 未含时限」。
- 最小修复：补一句「Z 的下界由固定 A/B 部分给出（B 型 5773.067），该路线的最优 Z 不代表可实现的全局改善」。

---

## 3. 结论（一句话）

**可以采信，但采信范围限定在「数值与可行性结论」，文档需先修订**：`results/问题二_改进方案/` 下四个口径方案的 80 箱唯一覆盖、载荷/体积/逐段剩余载荷/能耗/SOC/交付时刻/资源不重叠与硬时限、期望时间全部经我独立重算通过（四个方案的 makespan 与总能耗与文件全精度一致），`Pareto前沿_问题二.csv` 16 点互不支配、`完成时间上限实验.csv` 6 行可复算，「固定组批/路线/机型下仅重排不可能突破 5903.038155 s」经 2^6 枚举与逐机型下界复核成立且文档明确声明其非全局下界；**但**对比报告 §4 的原主方案硬时限余量数字错误（216.953/S004-MED-01 → 应为 204.170/S012-WAT-01）、§2 的「改善主要来自放开 4 站」缺乏消融支撑（所有已产出方案每架次最多 2 站）、结果说明中的「逐机型精确调度」与产物不符，均须按 H-1 / H-2 / M-1 修订后方可对外提交 —— 因此本轮审阅结论为 **needs_revision（不阻塞已发布结果文件的使用与下游计算，仅阻塞文档表述）**。

---

## 附：本轮执行的独立命令与脚本（均在 `%TEMP%`，未改动仓库）

| 目的 | 命令 / 脚本 |
|---|---|
| 官方验证入口 | `python code/问题二_改进求解\q2_verify.py` → PASS 4/4 |
| 独立全量复算（xlsx + 几何表 + 自写公式） | `%TEMP%\review_indep.py` |
| 导出 CSV 与 JSON 一致性、cap_best 与 Pareto 复核 | `%TEMP%\review_export.py`、`%TEMP%\review_final.py`、`%TEMP%\review_margin.py` |
| 逐机型全排列可行性枚举（检验 feas_sched 是否生效） | `%TEMP%\review_feas2.py` |
| 各方案二划分下界 | `%TEMP%\review_part.py` |
| 负结果重跑 | `%TEMP%\q2neg\joint_recon.py`、`%TEMP%\q2neg\pool_opt.py` |
| 语法级对比 | `difflib.unified_diff(问题二_求解.py, 问题二_改进求解\q2_solver4.py)` + SHA-256 |
---

# 第二轮复审（t2，2026 修订后复检）

- 复审范围：t2 指定的 H-1 / H-2 / M-1 / M-2 / M-3 / L-1～L-5 共 10 项修复；仍**未修改任何方案数值**（所有重跑输出写在 `%TEMP%`，唯一改动是本报告文件）。
- 时点事实：`results/问题二_改进方案/` 下 4 个方案 × 4 个文件（共 16 个）已被 `apply_feas.py` 重新导出（git 显示均为 modified），另新增 `Pareto前沿_问题二_specs.json`。
- 复审命令（全部实跑）：`python code/问题二_改进求解\q2_verify.py`、自写独立复算脚本（xlsx + 几何表 + 自写附录 2 公式，不使用 `问题二_调度核心.segment`/`load_data`）、自写逐机型全排列可行性枚举、`python code/问题二_改进求解\ablation_3v4.py 40000 6 14 %TEMP%\ablation.json`、`python code/问题二_改进求解\parallel4.py 1 1 1 %TEMP%\p4_probe.json`、`python code/问题二_改进求解\parallel4b.py 1 1 1 %TEMP%\p4b_probe.json`、以及在 `%TEMP%\r2\`（含 `runs/` 副本）内重跑 `joint_recon.py` / `pool_opt.py` / `cap_summary.py`；未运行 `finalize4.py` / `front_final.py` / `export_caps.py`（它们会覆写发布结果文件），改为用自写脚本复现其选取与前沿逻辑。

## 0. 判定一览

| 条目 | 判定 | 一句话依据 |
|---|---|---|
| H-1 | **部分修复** | 原主方案一行已更正为 204.170（S012-WAT-01，与我复算一致）；但同表「新方案 144.170」与第 44 行结论因重新导出而失效（新值 337.293），叙述方向反转 |
| H-2 | **已修复** | 单变量消融实跑：3 站与 4 站两臂最优指标完全相同（5903.038155 s / 59.465812 kWh / 18 架次）；文档归因已改为「长时/多起点搜索，4 站容量未生效」 |
| M-1 | **部分修复** | 4 个发布方案的 B/C 顺序已全部逐机型最优、makespan/能耗不变、`q2_verify.py` PASS 4/4；但重新导出后 `结果说明` §二/§三 未同步（24/24 行过期） |
| M-2 | **已修复** | `parallel4.py` / `parallel4b.py` 初值文件名已修正且实跑通过；README §3 复现链已补全 |
| M-3 | **已修复（1 项 low 残留）** | runs/ 已入库；pool 1564、joint 565、cap 404、Pareto 16 点、发布四方案的 specs 全部可从仓库内产物独立重算 |
| L-1 | **已修复** | `q2_verify.py` 文档串不再声称检查 CSV 舍入 |
| L-2 | **已修复** | README 已写「唯一的两处**语义**改动」；`q2_solver4.py` 首字节已无 BOM，diff 仅两处上限 + 尾部空行 |
| L-3 | **已修复** | `validate_export.py` 已在 `问题二_改进求解\legacy\`，顶层不再存在 |
| L-4 | **已修复** | README §7.3 已指向 `results\建模算法审计\12_独立核验原始输出.json` 的 `元信息.口径` |
| L-5 | **已修复** | README §6 已注明 Z 下界由固定 A/B 决定（B 型 11546.133/2 = 5773.067） |
## 1. H-1 / H-2 实测证据

### H-1 部分修复

- **已更正部分（成立）**：`对比报告.md:40` 现为「原发布主方案 | 21.87% | 204.170（S012-WAT-01）」。我独立复算 `results/问题二_参考口径/主方案_完整方案.json` 的 31 个硬截止箱：最小余量 = `S012-WAT-01` 的 **204.169936 s**（交付 3395.830064 / 截止 3600），`S004-MED-01` 为 **1621.889984 s** —— 与更正后的数字一致，且与参考口径自身说明（204.170）一致。
- **未同步部分（新问题，见 R-1）**：同表「新时间优先主方案 | 20.137% | **144.170**（S012-WAT-01）」与第 44 行结论「用更小的硬时限最小余量（204.170→144.170 s）换取改善」是按 **apply_feas 重新导出前**的旧排程写的。按当前发布文件独立复算：
  - 主方案最小硬时限余量 = **337.292931 s**，箱号为 `S007-MED-01` / `S007-WAT-01`（交付 3262.707069）；
  - `S012-WAT-01` 现由 T06 交付，交付时刻 **1334.475037**、余量 **2265.524963 s**（旧值 3455.830064 / 144.2）；
  - 新方案最小余量 337.29 s **大于**原主方案的 204.17 s，故「用更紧的余量换取改善」的方向已不成立。
- 其余相关数字仍成立：最低返航 SOC 21.87% → 20.137%（−1.73 pp）✔；硬时限零违反、加权延误零 ✔。

### H-2 已修复

- 复查 `ablation_3v4.py`：任务表 = 2 个求解器 × 3 个起点（发布的时间优先/均衡/能耗优先方案）× 2 个权重（完成时间优先、能耗优先）× 6 个种子（8000–8005），每个 40000 迭代 → 两臂各 36 个任务，**唯一变量是 `问题二_求解.py`(3 站) 与 `q2_solver4.py`(4 站) 的站点上限**（两者 diff 已确认为两处上限）；起点、种子、预算完全相同，构成合法的单变量消融。
- 我实跑 `python code/问题二_改进求解\ablation_3v4.py 40000 6 14 %TEMP%\ablation.json`（为不改动仓库，输出改到 %TEMP%；脚本与预算与任务书给的一致）：72 任务，**206.7 s 完成**，两臂各 36 条记录，复算得

| 臂 | min 完成时间 / s | min 能耗 / kWh | min 架次 | 各指标最优解最大站数 | 全臂最优解中含 ≥3 站的架次数 |
|---|---:|---:|---:|---:|---:|
| S3（3 站） | 5903.038155 | 59.465812 | 18 | 2 | 1 |
| S4（4 站） | 5903.038155 | 59.465812 | 18 | 2 | 0 |

- 与 `对比报告 §2` 消融表（5903.038 / 59.466 / 18 两臂相同；「最优解最大站数」3 站→3、4 站→2）一致：我按「全臂所有最优解」口径复算，S3 臂最大站数 = 3、S4 臂 = 2 ✔。
- 结论：**4 站容量在本轮未生效**，两臂指标相同；README §1/§7.1 与对比报告 §2 已把归因改为「改善来自长时、多起点并行搜索；4 站容量未生效」，与证据一致 ✔。
- 提示（不构成问题）：该消融只覆盖 3 个起点 × 2 个权重的同预算对照，其结论仅限于该预算范围（文档已用「本轮」措辞）。
## 2. M-1 / M-2 / M-3 实测证据

### M-1 部分修复

**已修复的部分（全部实测通过）**

- **逐机型顺序已真正最优**：用与 `feas_sched.py` 同口径的全排列 + 硬时限/延误可行性过滤枚举各机型全部顺序，四方案的 B/C 现在都满足 `published == min feasible order`：

| 方案 | B 型 k | published B 最大返回 / s | 最优可行顺序 / s | C 型 k | published C / s | 最优可行顺序 / s |
|---|---:|---:|---:|---:|---:|---:|
| 时间优先(主方案) | 7 | 5812.714096 | 5812.714096 | 6 | 5903.038155 | 5903.038155 |
| 均衡 | 8 | 6766.947112 | 6766.947112 | 7 | 7299.970162 | 7299.970162 |
| 能耗优先 | 8 | 7174.839902 | 7174.839902 | 8 | 8237.565447 | 8237.565447 |
| 架次优先 | 8 | 7063.090854 | 7063.090854 | 8 | 8171.565447 | 8171.565447 |

  （上一轮 `架次优先/均衡/能耗优先` 的 B 型分别可缩短 627.8 / 92.1 / 557.6 s，现已全部消除。）
- **指标不变**：独立复算四方案（自写公式 + 自写调度器，未调用 `segment`/`load_data`）：
  `5903.038154612774 s / 65.89095992013699 kWh`、`7299.970162268759 / 61.709747853750436`、`8237.565447127903 / 59.4658115950177`、`8171.565447127903 / 60.44180693383757`，与首轮完全一致；`hard=0`、`tardy=0`、最低 SOC 0.201373 / 0.205652 不变；80 箱唯一覆盖、质量体积、逐段剩余载荷、逐箱交付时刻、无人机/电池区间不重叠等 4 方案全部 `ERRORS: none`。
- **`q2_verify.py` → PASS 4 / 4**；三张导出 CSV（逐架次/逐箱交付/逐航段）与重新导出的 JSON 在 6/3 位舍入下一致（24/80/50、20/80/42、18/80/39、18/80/39 行，`errors=none`）。
- **方法描述已改为真**：`结果说明` 首句现为「逐机型调度优化（B/C 全排列、A 局部搜索，已应用于发布方案）」，与 `apply_feas.py` 确实调用 `feas_sched.run` 的产物一致 ✔。

**未修复的部分（新问题 R-1）**

`apply_feas.py` 重新导出时**只写方案文件，没有同步文档表格**，导致：

- `结果说明` §二「主方案逐架次安排」（24 行）与当前 JSON **24/24 行不一致**，其中路线级互换的有：T01/T02（S001(2)↔S007(2)）、T06（S001(2)→S012(2)）、T10（S001(3)→S001(2)）、T16（S007(2)→S001(2)）、T18/T19（S001(2)↔S012(2)）、T21–T23（无人机/电池/时刻改变）；其余为亚秒级（如 T03 返回 1510→1510.472）。
- `结果说明` §三「硬时限余量最小的 6 个货箱」与末句「最小余量 144.2 s（S012-WAT-01）」过期：当前最小 6 箱为 `S007-MED-01 / S007-WAT-01` 337.293、`S014-MED-01 / S014-WAT-01` 455.734、`S012-MED-01` 485.108、`S001-WAT-01` 515.272。
- `对比报告` §4 见 H-1。

### M-2 已修复

- 代码：`parallel4.py:55` 与 `parallel4b.py:45` 均已改为 `方案_时间优先(主方案)_完整方案.json`。
- 实跑：`python code/问题二_改进求解\parallel4.py 1 1 1 %TEMP%\p4_probe.json` → `tasks 9`、`[9/9] best_time=5903.0 best_energy=59.466 best_sorties=18`、`done`、exit 0；`python code/问题二_改进求解\parallel4b.py 1 1 1 %TEMP%\p4b_probe.json` → `BEST {'makespan_s': 5903.038154612774, 'energy_kwh': 65.89095992013699, 'sorties': 24}`、exit 0。上一轮的 `FileNotFoundError` 已消除 ✔。
- README §3 复现链已补全为 parallel4 → parallel4b → ablation_3v4 → finalize4 → front_final → apply_feas → cap_summary → export_caps → q2_verify。其中我实跑或独立复现了：parallel4 ✔、parallel4b ✔、ablation_3v4 ✔、cap_summary ✔、q2_verify ✔，并用自写脚本复现了 finalize4 的四个选取规则与 front_final 的前沿算法 ✔（不直接运行这两个会覆写发布文件的脚本）。

### M-3 已修复（1 项 low 残留）

- `问题二_改进求解\runs\` 已入库 14 个 JSON（par4/par4b/prio/pareto/par_r1/par_r2/runs_a/runs_b + 6 个 cap_best）。
- **joint_recon**：在 `%TEMP%\r2\`（含 runs 副本，避免写仓库）重跑 → `candidate routes: 565 {'A': 83, 'B': 117, 'C': 365}`、`Z=5773.067`、解码 `n=25 T=6917.782 E=69.443 hard=4831.819296867143 tardy=237538.1`。仓库内随附的 `问题二_改进求解/joint_recon.json` 亦为 Z=5773.06669191599 / hard=4831.819296867143 —— 与 README 更正后的 **4831.8** 一致 ✔（首轮不可复现的 4859.4 已不再出现）。
- **pool_opt**：同法重跑 → `pool: 1564 {'A': 321, 'B': 183, 'C': 1060}` ✔，并跑到 `attempt 10  Z=5542.046  n=24  cuts=10  163.4s` ✔，与 README「池 1564 条、10 轮内 Z 降到 5542.046」一致。
- **404 个候选**：`cap_summary.py` 从 runs 读得 `candidates: 404` ✔，其 6 行上限表与发布 `完成时间上限实验.csv` 一致；我另用自写公式从 `runs/cap_best_*.json` 复算该表 6 行，`T/E/n/A-B-C` **逐行一致**（5903.038/65.8910/24；6132.337/64.6745/23；7846.229/60.0241/19；8237.565/59.4658/18 等）。
- **Pareto 16 行**：用自写公式 + 自写贪心调度器从 `runs/*.json` 独立重算：可行候选 **404**、非支配 **16 点**，16 点的 `(T, E, 架次)` 序列与发布 `Pareto前沿_问题二.csv` **完全相同（IDENTICAL = True）**；新增 `Pareto前沿_问题二_specs.json` 的 16 点逐点解码后与 CSV 各行一致 ✔。
- **发布方案可溯源**：按 `finalize4.py` 的四条选取规则从 runs 中重算，得到的候选其 specs 签名与四个发布方案**完全相同**、指标相同（时间优先←par4.json.time、均衡←par4.json.energy、能耗优先←par4.json.energy、架次优先←par4.json.sorties，4/4 签名相等）✔。
- **残留 low（R-2）**：runs 里的记录形态不统一 —— `par4b.json`（24 条）与 `pareto.json`（11 条）是扁平 `{specs, metrics}`，不含脚本期望的 `rec['time'/'energy'/'sorties']`，因此实际上不进入任何候选池；`par_r1.json` / `par_r2.json` / `runs_a.json` / `runs_b.json` 在全部脚本中**零引用**。即 14 个 shipped JSON 中只有 `par4.json`、`prio.json`（+ 6 个 cap_best）真正参与结果链。
## 3. L-1～L-5 实测证据

- **L-1 已修复**：`q2_verify.py:4` 现为「输出：每个方案一条 PASS/FAIL 记录（全精度 JSON）。CSV 舍入一致性由本脚本之外的独立检查完成。」——不再声称脚本内检查 CSV ✔（三张 CSV 与 JSON 的一致性本轮仍由我的独立脚本复核通过）。
- **L-2 已修复**：`q2_solver4.py` 首字节为 `"""`（BOM 已去除）；`difflib.unified_diff(问题二_求解.py, q2_solver4.py)` 只剩 `>3→>4`、`<3→<4` 两处上限与文件末尾一个空行 —— 与 README「唯一的两处**语义**改动」一致 ✔（残留空行不影响任何行为）。
- **L-3 已修复**：`问题二_改进求解\legacy\validate_export.py`（5218 B）已就位，顶层 `validate_export.py` 不存在 ✔；本轮未执行该脚本。
- **L-4 已修复**：README §7.3 现写「记录于 `results/建模算法审计/12_独立核验原始输出.json` 的 `元信息.口径`」✔（该文件确实含 P-01/P-02 条目：ΔE +0.0017% / ΔT −0.0168% / SOC +0.0043 pp）；对比报告 §4 也已写出 P-01=作业高度基准、P-02=DEM 像元语义 ✔。
- **L-5 已修复**：README §6 现写「该 Z 的下界由固定 A/B 部分决定：B 型 11546.133/2=5773.067，不代表可实现改善」✔，与我首轮的判断一致。

## 4. 第二轮仍存在的问题

### R-1（high）重新导出后文档数值未同步（H-1/M-1 的残留）

- 位置：`results/问题二_改进方案/问题二_改进方案_结果说明.md` §二（主方案逐架次安排表 24 行）与 §三（硬时限余量最小 6 箱 + 末句 144.2 s）；`问题二_改进求解/对比报告.md` §4 第 2 行与第 44 行结论。
- 事实（与当前发布文件对照，全部实测）：
  - §二 **24/24 行**与新 JSON 不一致：T01/T02、T06、T10、T16、T18/T19、T21/T22/T23 的访问顺序/无人机/电池/时刻发生互换（例：文档 T01=S001(2) 返回 1251，实际 T01=S007(2) 返回 1727.424；文档 T19=S012(2) 返回 4230，实际 T19=S001(2) 返回 3427.054），其余行为亚秒级差异；
  - §三 与对比报告 §4 的新方案余量 144.170 已失效：当前主方案最小硬时限余量 = **337.292931 s（`S007-MED-01`/`S007-WAT-01`，交付 3262.707069）**；`S012-WAT-01` 实际由 T06 交付于 **1334.475037**（余量 2265.524963）；
  - 结论方向亦失效：337.29 s > 原主方案的 204.17 s，不能再写「用更小的硬时限余量换取改善」。
- 影响：这是本轮唯一的阻塞项 —— 4 个方案数据文件本身经我独立复算完全正确，但**文档正文与发布文件互相矛盾**（24 行排程表 + 安全余量表 + 结论句），对外提交仍会自相矛盾。
- 最小修复：按新 JSON 重新生成 `结果说明` §二/§三（`apply_feas.py` 导出时可顺带生成，或手工用 `方案_时间优先(主方案)_逐架次.csv` / `_逐箱交付.csv` 重排），并把对比报告 §4 改为 `337.293（S007-MED-01）`、第 44 行结论改为「新方案在最紧硬时限余量与 SOC 上均不劣于/优于原主方案（SOC 由 21.87% 收紧到 20.137%，硬时限最小余量由 204.170 s 放宽到 337.293 s）」。

### R-2（low）runs/ 内记录形态不统一，README §3 的「因此」表述不准确

- 位置：`问题二_改进求解/README.md:34-36`；`问题二_改进求解/runs/par4b.json`、`pareto.json`、`par_r1.json`、`par_r2.json`、`runs_a.json`、`runs_b.json`。
- 事实：5 个消费脚本（finalize4 / front_final / cap_summary / joint_recon / pool_opt）都按 `rec['time'/'energy'/'sorties']` 取候选，只有 `par4.json`（108 条）与 `prio.json`（100 条）符合该形态；`par4b.json` / `pareto.json` 是扁平 `{specs, metrics}`，在该形态下**贡献 0 条候选**；`par_r1/par_r2/runs_a/runs_b` 未被任何脚本按文件名或通配引用（本轮 grep 计数 0），不参与候选池 —— 即「pool 1564 / joint 565 / 404 候选」实际只由 par4+prio（+ 基点方案）产生。
- 更正（重要）：这些文件并非无用 —— 其 `greedy`/`metrics` 字段保存真实实验点，可用于溯源。例如 `runs/par_r1.json` 第 8 条（seed 1004、40000 迭代、base=top14）恰为 `T=6027.234140 / E=69.089455 / n=25`，即 `对比报告` §2 的 **V1 行（6027.234 / 69.089 / 25）** —— 首轮判定为不可复现的 V1 数字现已可从仓库内产物溯源 ✔。
- 最小修复：README §3 把「因此候选池规模…可复现」改为「候选池由 `par4.json`+`prio.json` 生成；`par4b/pareto/par_r1/par_r2/runs_a/runs_b` 为原始实验留档（含 `metrics`/`greedy` 指标），不进入候选池」，或统一记录形态。

## 5. 第二轮结论（一句话）

**本轮 10 项修复中 8 项已修复、2 项（H-1、M-1）属「部分修复」**：数值与可行性（`q2_verify.py` PASS 4/4 + 我自写公式四方案复算全通过、makespan/能耗不变）、逐机型调度已真正最优、单变量消融（3 站=4 站=5903.038155 s / 59.465812 kWh / 18 架次，4 站容量未生效）、复现链（parallel4/parallel4b/ablation/cap_summary 实跑通过；pool 1564、joint 565 + Z=5773.067 + hard=4831.8、404 候选、Pareto 16 点与发布 CSV 逐点一致、发布四方案 specs 可从 runs 溯源）均已修复并经独立复核，第一轮 M-3 中 V1 数字的不可复现问题也随之消解；**但 `apply_feas.py` 重新导出后未同步 `结果说明` §二/§三 与 `对比报告` §4（24/24 行排程表过期、最小硬时限余量 144.170→337.293、结论方向反转），文档与发布文件仍自相矛盾**，故本轮审阅结论仍为 **needs_revision（阻塞范围仅限这 3 处文档表述，修复面很小且机械；4 个方案数据文件、Pareto 前沿、上限实验表与消融结论均可采信并可直接用于下游）**。

---

# 第三轮复审（t3：R-1a / R-1b / R-2 文档同步修复复检）

- 复审范围：仅第二轮遗留的 3 处文档同步问题 + 附带图件抽检 + 「未引入新数值改动」确认；前两轮已通过的内容不重做（仅作一致性抽检）。
- 方式：自写脚本逐行对照文档与当前 JSON、用原始 xlsx 独立复算余量/SOC、扫描残留字符串、抽取重生成 SVG 的内嵌文本、实跑 `q2_verify.py` 与全精度 metrics 比对。**未修改任何方案数值**（唯一改动是本报告文件）。

## 1. R-1a（结果说明整篇重生成）—— 已修复

- `results/问题二_改进方案/问题二_改进方案_结果说明.md` mtime 18:05:39（晚于方案 JSON/CSV 的 17:53）。
- **§二 逐架次安排表：24/24 行与当前 `方案_时间优先(主方案)_完整方案.json` 一致**。按文档整数显示口径（容差 0.5 s）、并把「→」归一到「-」后逐行比对：架次 id / 无人机 / 机型 / 电池 / 访问顺序 / 开始 / 返回 / 能耗 / SOC 全部一致，`bad=0`（第二轮为 24/24 行结构错位）。
- **§三 硬时限余量最小 6 箱：与我的独立复算逐行相同**：

| 文档（新） | 我独立复算（原始 xlsx + 自写公式） |
|---|---|
| S007-MED-01 337.293；S007-WAT-01 337.293 | 337.292931 / 337.292931（交付 3262.707069） |
| S014-MED-01 455.734；S014-WAT-01 455.734 | 455.733690 / 455.733690（交付 3144.266310） |
| S012-MED-01 485.108 | 485.107827（交付 3114.892173） |
| S001-WAT-01 515.272 | 515.271888（交付 3084.728112） |

  31 个硬截止箱全部满足、最小余量 337.292931 s（S007-MED-01）；**正文已无 `144.170` / `144.2` 残留**（机械扫描命中的 `144.2` 仅来自 `3144.266` 的子串，非旧值）。
- **§一 新增两列经独立复算全部一致**：最低 SOC = 20.137% / 20.137% / 20.565% / 20.565%（复算 0.201373 / 0.201373 / 0.205652 / 0.205652）；硬时限最小余量 = 337.293（S007-MED-01）/ 486.515（S002-WAT-01）/ 458.943（S012-MED-01）/ 552.515（S002-MED-01）（复算 337.292931 / 486.514730 / 458.943408 / 552.514730）。
- 主方案汇总句已改为「硬时限余量更大，但 SOC 余量更紧」；方法句去掉「最多 4 站」并把 4 站结论移入单变量消融注 —— 与第二轮结论一致 ✔。

## 2. R-1b（对比报告 §4）—— 已修复

- §4 表现为「原发布主方案 | 21.866% | 204.170（S012-WAT-01）」「新时间优先主方案 | 20.137% | **337.293（S007-MED-01）**」—— 两个数字与我的独立复算一致（参考主方案 204.169936 s @S012-WAT-01；新主方案 337.292931 s @S007-MED-01）✔。
- 第 44 行方向已纠正：「用**更紧的 SOC 余量（21.866%→20.137%）**换取时间与能耗改善；**硬时限最小余量反而更大（204.170→337.293 s）**」✔ —— 第二轮的「方向反转」问题消除。
## 3. R-2（README §3 表述）—— 已修复

- README §3 现明确：「**候选池/选择脚本实际消费的是 `par4.json` + `prio.json`**（记录格式为 `{time,energy,sorties}`）；`par4b.json`、`pareto.json`、`par_r1.json`、`par_r2.json`、`runs_a.json`、`runs_b.json` 为历史/辅助记录（格式不同，不被选择脚本消费，其中 `par_r1.json` 可溯源 V1=6027.234/69.089/25）；`cap_best_*.json` 为上限实验输出。候选池规模（pool_opt 1564 条、joint_recon 565 条）与 404 个候选由前两者可复现」。
- 与我第二轮的实测完全一致：5 个消费脚本（finalize4/front_final/cap_summary/joint_recon/pool_opt）均按 `rec['time'/'energy'/'sorties']` 取候选，只有 par4.json（108 条）与 prio.json（100 条）符合；par4b/pareto 为扁平 `{specs, metrics}` 形态贡献 0 条；par_r1/par_r2/runs_a/runs_b 无脚本引用（其 `greedy` 字段留档，par_r1 第 8 条 = V1 点）✔。表述与实现一致，不再暗示「全部 runs 因此可复现」。

## 4. 附带图件抽检 —— 通过

- 三张图 `result_q2_uav_gantt` / `result_q2_battery_gantt` / `result_q2_hard_margin`（svg+png）mtime 18:05:52–53，晚于当前 CSV（17:53:02）与重导 JSON（17:53），确按重导后时序重生成。
- 用 SVG 内嵌文本抽检（矢量图文字可直接读取）：
  - `uav_gantt.svg`、`battery_gantt.svg` 标题均含「**逐机型重排后**」✔；
  - `hard_margin.svg` 标题为「主方案 31 个硬时限货箱的送达余量（最小 **337.3** s）」，与独立复算的最小余量 337.292931 s 一致；图中 31 个箱号标签按余量升序排列，最前 6 个正是 S007-MED-01、S007-WAT-01、S014-MED-01、S014-WAT-01、S012-MED-01、S001-WAT-01，与 §三 及当前 `_逐箱交付.csv` 完全一致 ✔。
- 提示（low，非阻塞、非本轮修订项）：`figures/问题二_改进方案/` 的三张图在仓库内**没有对应的生成脚本**（`问题二_参考口径绘图.py` 只写 `figures/问题二_参考口径`），因此图件只能靠内嵌文本抽检，不能从仓库重跑生成。

## 5. 未引入新数值改动 —— 抽检通过

- 四方案 metrics 与任务书给定全精度值逐一比对（`abs(diff) < 1e-9`）：`5903.038154612774 / 65.89095992013699 / 24`、`7299.970162268759 / 61.709747853750436 / 20`、`8237.565447127903 / 59.4658115950177 / 18`、`8171.565447127903 / 60.44180693383757 / 18` —— **4/4 True**。
- `python code/问题二_改进求解\q2_verify.py` → **PASS 4 / 4**（均衡 20/7299.970/61.7097；主方案 24/5903.038/65.8910；架次优先 18/8171.565/60.4418；能耗优先 18/8237.565/59.4658）。
- 方案 JSON/CSV 的 mtime 仍为 17:53（本轮仅 md 文档与 figures 更新），确认修订只触及文档、未改动方案数值。

## 6. 第三轮结论（一句话）

**R-1a / R-1b / R-2 三处文档同步问题已全部修复**：结果说明 §二 24/24 行、§三 最小 6 箱（337.293 s @S007-MED-01，无 144.170 残留）与 §一 新增两列均与当前 JSON 独立复算一致，对比报告 §4 数字与方向已纠正，README §3 表述与实际脚本消费（par4+prio）一致，三张重生成图件的标题/最小余量与当前 CSV 一致，四方案数值无任何改动、`q2_verify.py` 仍 PASS 4/4 —— **现在可以对外提交**。采信范围：`results/问题二_改进方案` 的 4 个方案数据文件、Pareto 前沿与 `Pareto前沿_问题二_specs.json`、完成时间上限实验表、结果说明/对比报告/README 的现行表述，以及单变量消融结论（改善来自长时/多起点搜索，4 站容量未生效）；唯一非阻塞提示是改进方案图件缺少随仓库的生成脚本（low）。
