# 山区洪涝灾害无人机运输与通信协同优化

本仓库保留 D 题四问的当前求解代码、主方案、必要的指标权衡和核验材料。所有时间均以任务开始为零点、单位为秒；原始题目附件与 30 m DEM 不随仓库分发。

| 问题 | 当前方法 | 主结果 | 详细说明 |
|---|---|---|---|
| 一 | 逐箱直接指派 MILP 与能耗切平面 | 18 架次，59.130290 kWh | [问题一结果](results/问题一_非枚举整数规划/结果说明.md) |
| 二 | 异构无人机、电池联合调度与邻域搜索 | 25 架次，6410.481 s，69.965 kWh | [问题二结果](results/问题二_参考口径/问题二结果说明.md) |
| 三 | DEM 连续链路证书与中继联合调度 | 22 运输架次、4 中继架次，通信缺口 0 | [问题三结果](results/问题三_参考口径/问题三结果说明.md) |
| 四 | 固定问题三计划的分区资源 MILP | 两组缺口 2 件，三组缺口 4 件 | [问题四结果](results/问题四_参考口径/问题四结果说明.md) |

最终提交用的[四问工作簿](results/问题四_参考口径/结果提交_问题三四主方案.xlsx)将 Q1 的非枚举主方案、Q2–Q4 的当前主方案合在同一模板。逐问结果和独立审计位于对应的 `results/问题…_参考口径/` 或 `results/问题一_非枚举整数规划/`；图表位于 `figures/` 下对应目录。Q2 的非支配候选和 Q4 的工作量均衡备选保留用于解释目标权衡，不能误作替换当前主方案的旧版本。

## 仓库结构

| 目录 | 内容 |
|---|---|
| `code/` | 全部 Python 代码（扁平放置，脚本间以模块名互相 import） |
| `code/问题二_改进求解/` | 第二问改进实验：22 架次/5792.101 s、23 架次/5903.038 s、单目标极值等，含 `runs/` 原始记录 |
| `code/问题三_改进求解/` | 第三问改进实验：4 中继窗口优化（8914.080 s，已验证）、3 中继候选（8027.471 s，待连续认证）、严格逐点/连续认证脚本 |
| `code/问题三_成果呈现/` | 问题三成果呈现与权重分析脚本 |
| `code/utils/plot_style.py` | 绘图公共样式 |
| `docs/` | `题目分析报告.md`、`术语表格.md`、`使用指南.md` |
| `results/` | 各问结果、独立审计、建模修复与数值复核记录 |
| `figures/` | 图件（仅 PNG；灰度预览与 SVG 不入库） |
| `paper/` | 论文草稿与生成脚本（原 `.paper_work/`） |
| `数据/` | 原始附件与 30 m DEM（不入库；复现时放回仓库根目录） |

> 外部参考资料（`2026华为杯数模研赛D题解题过程.pdf`、`第二十三届华为杯latex模板…`）已移出仓库到同级 `..\_external\`，不随仓库分发。

## 口径说明与待决项

- **作业高度基准**：问题二至问题四的节点作业高度取原题附件《调度中心与服务区.xlsx》的节点表地面海拔 + 30 m；问题一的几何缓存（`results/航路节点坐标与作业高度.csv`、`results/单服务区往返几何参数.csv`）仍按 DEM 像元地面高程 + 30 m 生成，因此 `results/有向航段几何参数.csv` 与 `results/问题二_参考口径/有向航段几何参数.csv` 是作业高度基准不同的同名双版本（240/240 条爬升/下降不同，最大 10.563 m，计划巡航海拔相同）。按同一组批重算，切到节点表基准后问题一已发布指标变化为 ΔE = +0.000996 kWh（+0.0017%）、ΔT = −5.494 s（−0.0168%）；统一口径需重解问题一并重写四问结果工作簿，且时间相对变化超过 0.01% 复核阈值，故登记为待决项，本轮不做静默改数。量化脚本：`results/建模修复/检验作业高度口径.py`。
- **DEM 像元采样语义**：原始 GeoTIFF 的 `GTRasterTypeGeoKey`(1025) = 2（`RasterPixelIsPoint`），而问题三的中继选点与巡航海拔生产路径仍使用 `问题三_通信核心.Terrain` 的 floor 索引加像元中心（Area 口径）；问题三的通信证书已用 `PointRasterTerrain` 语义重新认证（`问题三_全程通信重认证.py`，0.5 s 分辨率、未认证 0 s）。切换采样语义会改变节点高程（1 像元内最大 10.423 m）、中继悬停海拔与覆盖矩阵，实质影响问题三/四数值，登记为待决项。
- 两条待决项的影响范围、建议方案与全部 finding 处置见 [`results/建模修复/04_修复清单.md`](results/建模修复/04_修复清单.md)。

- **中继选点脚本（D15/P-03，待裁决）**：`问题三_中继位置微调.py` **无源码**（仅 `__pycache__/*.pyc`）。本仓库表述应为「**方案使用 3 个静态点位；选点脚本缺失，最小性未独立判定**」，点位以 `results/问题三_参考口径/候选悬停点覆盖.csv`（3102 行）+ `问题三_联合调度.SITES` 断言 + 全流程复算为准。
- **结论强度（F-14/E6）**：本仓库结论只支持「**独立重算一致**」（两套独立实现 + 已发布产物复算）；**未做**端到端生成链隔离重跑（`航路几何数据.py`→Q1→`问题二_复现.py`→`问题三四_复现.py`），不主张「逐问复现」。
- **论文 docx（P-04）**：`.paper_work/md_to_docx.py` 无法整篇复现现行 docx（t5 只做段落级同步）；docx 视为**离线维护产物**，重生成不等价。
- **指纹基线（F-13）**：`results/数值实验复核/实验清单.json` 的 275 条指纹**冻结于 t1 时点**；文档类条目已在 t5/t7 修复轮变更，逐条原值→新值记录见 [`results/建模修复/05_提交物修复清单.md`](results/建模修复/05_提交物修复清单.md)。

## 复核入口与提交物指引（t7）

- **复核入口（单一事实来源）**：[`results/建模算法审计/13_复核裁决.md`](results/建模算法审计/13_复核裁决.md)（review-2 裁决 needs_revision，blocker=0/high=3/medium=8/low=4；finding F-01–F-15；待决项 P-01–P-09）；判据基准 [`results/建模算法审计/01_审阅基准.md`](results/建模算法审计/01_审阅基准.md)（§9 共 33 条）。t7 的修复与验证记录见 [`results/建模修复/05_提交物修复清单.md`](results/建模修复/05_提交物修复清单.md)。
- **以 `results/问题四_参考口径/结果提交_问题三四主方案.xlsx` 为准**（6 表：`Q1_单点组批`19 / `Q2_运输架次`23 / `Q2_逐箱交付`81 / `Q3_中继架次`5 / `Q3_通信保障`251 / `Q4_分区配置`6 行）。
- `results/问题一_非枚举整数规划/结果提交_问题一主方案.xlsx` 除 `Q1_单点组批`（19 行）外的 5 张表**均不得作为提交内容**（F-09），实测口径如下（`openpyxl` 只读核对，N-04）：
  - `Q3_中继架次`/`Q3_通信保障`/`Q4_分区配置` 三表（29/79/58 行）与 `问题二_参考口径/结果提交_主方案.xlsx` 的**同名透传表逐格一致**，但**不等于**当前 Q3/Q4 主方案（`结果提交_问题三四主方案.xlsx` 为 5/251/6 行）；
  - `Q2_运输架次`/`Q2_逐箱交付` 两表仅 **3/3 行**，与 `问题二_参考口径/结果提交_主方案.xlsx` 的 **26/81 行不一致**，属更早的上游模板残留。删除或重建这些工作表会使 `问题二_独立审计.py:162` 的「原工作表一致」断言失败（已复现 `KeyError: Q3_中继架次`），故本轮改走「文档标注 + 提交声明」路径：以本节的 6 表工作簿为准，结构性修复（`fill_template` 同步 + 审计脚本同步改）登记为待办。
- 收敛记录口径：`搜索收敛记录.csv` 为「每 100 次一记、该次迭代无可行候选即缺号」，1019 行/20 组/每组 42–61 条，**不是** 20×80 完整轨迹，不得用作 8000 次迭代收敛的充分证据。

## 复现

将原始 `数据/` 目录放在仓库根目录，保持原文件名和层级；几何重建需要原始 DEM `.mat`，通信验证需要同一 DEM 的 `.tif`。需要 Python 的 `numpy`、`scipy`、`openpyxl`、`pandas`、`pillow`、`matplotlib`；绘图使用本地 math-modeling Skill 的导出工具和中文字体。按顺序运行：

```bash
python code/航路几何数据.py
python code/问题一_直接指派整数规划.py --sensitivity
python code/问题一_直接指派审计.py
python code/问题二_复现.py --iterations 8000 --seeds 0 1 2 3 4
python 问题三四_复现.py
```

问题一求解器会同步生成当前 Q1 工作簿。问题二据此填写 Q2，问题三填写 Q3，问题四填写 Q4。若只核对已发布结果，可分别运行三个独立审计脚本：`问题一_直接指派审计.py`、`问题二_独立审计.py`、`问题三四_独立审计.py`。Q1 的现成数据图保留在仓库中，`问题一_直接指派绘图.py` 重绘其 MILP 过程图，并调用 `问题一_流程图.py` 重绘问题一子问题流程图（`flow_q1_model.svg` / `.png` / `_grayscale.png`）。

本仓库的启发式主方案不声明全局最优；问题四的 MILP 最优性只针对固定的问题三计划。旧方案可从 Git 历史查看，不与当前工作簿混用。
