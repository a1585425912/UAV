# D题问题一与问题二：运输能力、组批和多点调度

## 范围与数据合同

本节只处理第一问。题目要求：计算 A/B/C 三机型到 15 个服务区的最大安全载荷；将 80 个不可拆货箱仅在同一服务区内组批、每箱交付一次；在全部交付前提下优化架次、能耗与累计作业时间；分析返航安全余量。第一问明确忽略实体无人机数、共享电池调度和通信限制，因而累计作业时间是所有架次独立作业时间之和，不是并行完成时刻，也不能用于判断第二问的逐箱时限。

只读输入为题目 DOCX、`数据/无人机应急物资运输基础数据/调度中心与服务区.xlsx`、`物资需求与配送时限.xlsx`、`运输无人机数据.xlsx`、`数据/镇龙乡地理空间数据/镇龙乡及周边地理数据/数字高程模型数据（DEM）/镇龙乡及周边30米DEM.mat`。结果写入 `results/`，不覆盖题目附件或结果提交模板。服务区 15 个，逐箱清单 80 行；货箱质量、体积、服务区和编号均直接取逐箱清单，汇总需求表用于核对，不按汇总表扩充货箱。DEM 为 WGS84 约 30 米栅格，NoData 为 −32767；节点经纬度和高程来自节点表。三机型参数来自运输无人机表。

| 合同项 | 第一问定义 |
|---|---|
| 结论 | 45 个机型—服务区最大安全载荷；每架次机型、服务区、货箱清单、质量、体积、往返时间、能耗和返航 SOC；总架次、总能耗、累计时间及余量变化 |
| 模型 | DEM 约束的单点往返能量模型 + 服务区独立的整数覆盖组批模型，共两个串联模型族 |
| 目标 | 先最小化架次数，在最少架次可行方案中最小化总能耗，再在前两者最优的方案中最小化累计时间；明确这一优先关系及其理由，并报告余量变化时三项指标的变化 |
| 硬约束 | 每箱恰好一次；每架次只去一个服务区并返 O01；质量、体积和能量余量均不超限 |
| 验证 | 与汇总需求核对 80 箱；逐箱覆盖与唯一性；逐航段量纲、DEM 覆盖、SOC 与余量；整数规划求解状态和目标复算 |
| 风险 | 题面只明确总能耗分为水平航程能耗与爬升附加能耗，未单独写出这两项的闭式表达；下述等效航程与势能换算口径须在提交前由队伍复核 |

## 航段几何与能量

对每个 O01—服务区的水平直线航段，用 DEM 像元边界与线段交点划分区间，并读取每段中点所在像元，取得所有经过像元的最高高程 $z_i^{\max}$。巡航海拔 $H_i=z_i^{\max}+50\,\mathrm{m}$。O01 作业高度为节点地面海拔 $z_O$，服务区作业高度为 $z_i+30\,\mathrm{m}$。去返程共用同一水平线段及巡航海拔，但上升、下降高度分别按各自起终点高度计算。水平距离用 WGS84 大地测量短距离近似或椭球测地距离，程序固定一种口径并记录。

机型 $g$ 在载荷 $q$ 下的等效航程按题面公式

$$L_g(q)=L_g^0-(L_g^0-L_g^F)(q/Q_g)^{3/2},\quad 0\le q\le Q_g.$$

水平航程能耗采用与该等效航程一致的比例换算 $E^{\mathrm{hor}}_g(d,q)=E_g^{\mathrm{use}}d/L_g(q)$，其中标准航程 $L_g(q)$ 的单位为 m，故 $d/L_g(q)$ 无量纲，再乘 kWh 的 $E_g^{\mathrm{use}}$；爬升附加能耗采用重力势能除以效率 $E^{\mathrm{up}}_g(h,q)=(m_g^0+q)\,9.81h/(3.6\times10^6\eta_g)$，单位 kWh。统一航段结果采用 $g_0=9.81\,\mathrm{m/s^2}$，与所附问题一计算任务说明一致；分项换算属于建模假设。$m_g^0$ 为含电池空载总质量（kg），$\eta_g$ 为附件爬升能耗效率（无量纲）。下降效率为 0，故不记额外下降能耗。去程载荷为 $q$，返程载荷为 0。定义 $h_{Oi}^{+}=H_i-z_O$、$h_{iO}^{+}=H_i-(z_i+30)$，总能耗 $E_{gi}(q)=E^{\mathrm{hor}}_g(d_i,q)+E^{\mathrm{up}}_g(h_{Oi}^{+},q)+E^{\mathrm{hor}}_g(d_i,0)+E^{\mathrm{up}}_g(h_{iO}^{+},0)$。

安全条件为 $E_{gi}(q)\le(1-\rho_g)E_g^{\mathrm{use}}$ 且 $q\le Q_g$。由于 $L_g(q)$ 随 $q$ 非增、爬升项随 $q$ 非减，$E_{gi}(q)$ 单调非减，故先检查空载可达性，再用二分法求最大连续安全载荷。此值是质量上界；可装货箱还需体积约束。

令 $v_g^{\uparrow},v_g^{\mathrm{c}},v_g^{\downarrow}$ 分别为机型爬升、巡航和下降速度，$h_{Oi}^{-}=H_i-(z_i+30)$、$h_{iO}^{-}=H_i-z_O$。往返飞行时间为

$$t_{gi}^{\mathrm{fly}}=\frac{h_{Oi}^{+}+h_{iO}^{+}}{v_g^{\uparrow}}+\frac{2d_i}{v_g^{\mathrm{c}}}+\frac{h_{Oi}^{-}+h_{iO}^{-}}{v_g^{\downarrow}}.$$

本题同一航线去返程爬升与下降高度数值互换，故上式可以简化为两倍共同垂直距离除以各速度，但程序仍分别核算航段。$n$ 箱的总作业时间为 $t_p=t_{gi}^{\mathrm{fly}}+t_g^{\mathrm{prep}}+nt_g^{\mathrm{load}}+t_g^{\mathrm{handoff}}+nt_g^{\mathrm{extra}}$。其中 A/B 型分别为 300+30$n$+150+30$n$ 秒；C 型为 300+30$n$+180+36$n$ 秒。返航荷电状态 $\mathrm{SOC}_p=100(1-e_p/E_g^{\mathrm{use}})\%$，须满足 $\mathrm{SOC}_p\ge100\rho_g\%$。第一问没有实体资源顺序与周转调度，故不加电池充电时间。

## 组批整数模型与求解

现行非枚举方法对每个服务区建立逐箱直接指派混合整数规划。令 $x_{btg}\in\{0,1\}$ 表示货箱 $b$ 由第 $t$ 架次、机型 $g$ 运输，$y_{tg}\in\{0,1\}$ 表示启用该架次机型。约束 $\sum_{t,g}x_{btg}=1$ 确保每箱恰交付一次，$\sum_g y_{tg}\le1$ 确保每架次只选一种机型；每架次的质量、体积、真实能耗分别受 $Q_g$、$V_g$、$(1-\rho_g)E_g^{\mathrm{use}}$ 约束。服务区独立求解，不进行跨区组批。初始架次上界由一个可行贪心装箱给出，但最终方案由整数规划决定，贪心不参与最优性判断。

能耗 $E_{gi}(q)$ 随载荷递增且为凸函数。程序对每个活跃的机型—架次建立能耗辅助变量，在当前载荷点增加切线作为全局下界；求解 MILP 后用原始非线性能耗式复算每架次，若约束不满足或真实总能耗与 MILP 下界差超过 $10^{-6}$ kWh，就加入新切平面再求。这样无需预先生成货箱子集，停止时既有真实可行解，也有求解器给出的目标下界和数值间隙。对空载不可达的机型，同时禁用该机型架次变量及对应货箱指派变量。

三级词典序优化：先最小化 $\sum_{t,g}y_{tg}$，再固定最少架次数、最小化真实总能耗，最后在能耗最优值 $10^{-6}$ kWh 容差内最小化累计作业时间。各服务区无共享约束，因此分区词典序最优解拼接后仍为全局最优。若 MILP 未证明最优、真实能耗复核失败或外逼近未收敛，不将结果写成已证最优。

返航余量取基准 20% 和若干情景值，重新求 45 个载荷与组批；报告可达性、总架次和指标变化。超过空载可行阈值的机型—服务区标记不可达，不把 0 kg 与可行 0 kg 混淆。

## 假设、依据与失效条件

1. **关键**：每架次严格执行 O01→单个服务区→O01，交付后返程载荷为零；依据题面第一问。若允许多点航线，必须转入第二问模型。
2. **关键**：DEM 最高像元加 50 m 为整个航段巡航海拔；依据题面附录 2。若改变净空标准，须重算 45 个机型—服务区能量曲线。
3. **关键**：单组满电的可用能量及返航余量按题面附件判定；依据题面附录 2。变化时重算安全载荷与全部模式。
4. **可放宽**：采用 $E^{\mathrm{use}}d/L_g(q)$ 作为水平能耗、势能效率换算作为爬升附加能耗；这是对题面文字的显式实现口径。若官方给出更详细公式，应以官方公式替换并重跑。现有无人机能耗文献只支持“负载影响能耗”的建模动机，不能替代题面参数。
5. **可放宽**：各架次不考虑风、天气变化及起降绕飞；第一问题面只给直线航段。可用距离/能耗扰动做稳健性复验。

## 复现与图表计划

Python 依赖 `numpy`、`scipy`、`openpyxl`、`matplotlib`。读取时显式核对逐箱表首末行、80 箱、15 区、字段非空与类型内一致性。最小可运行切片先验证一个服务区的航段、最大载荷、逐箱指派和完整性，再全量求解。输出 CSV、JSON 和图表；如需填写题目模板，再另存 XLSX。原始图展示地形、需求和机型参数；过程图展示能量—载荷边界及 MILP 切平面调用；结果图展示安全载荷、架次和返航 SOC。余量敏感性以 CSV 和结果说明中的数值表呈现。流程图覆盖输入—航段/能量—直接指派—切平面迭代—独立核验—输出。

## 参考依据

- 题目 DOCX 附录 2、运输无人机数据和 DEM 说明是本题计算规则与参数的权威输入。
- Dorling, K. et al. (2017), *Vehicle Routing Problems for Drone Delivery*, IEEE TSMC: Systems 47(1), 70–85, DOI: [10.1109/TSMC.2016.2582745](https://doi.org/10.1109/TSMC.2016.2582745)，用于说明载荷依赖能耗和多架次规划的研究背景。
- Zhang, J. et al. (2021), *Energy Consumption Models for Delivery Drones: A Comparison and Assessment*, Transportation Research Part D 90, 102668, DOI: [10.1016/j.trd.2020.102668](https://doi.org/10.1016/j.trd.2020.102668)，用于说明无人机能耗模型应明确其适用口径。

# 问题二：异构无人机多点多架次运输调度

## 题目、数据与结论合同

问题二允许每架次由 O01 出发访问一个或多个服务区，最终回 O01，忽略通信。需联合决定 80 个不可拆货箱的架次与交付站、站点顺序、A/B/C 机型、8 架实体无人机、14 组同型共享电池及每架次开始时刻；交付时刻必须按航段飞行和现场交接逐箱落实。运输无人机表给出 A 型 4 架/6 组电池、B 型 2 架/4 组、C 型 2 架/4 组，均从 O01 满电出发。需求逐箱清单 80 行，15 区，其中饮用水 36、食品 19、医疗 16、卫生用品 9；30 箱标记首批。按题面判定，医疗物资的期望时间和首批截止时间取较早者作为硬约束，其余首批货箱也有硬截止，共 31 箱有硬时限（3600 s 为 17 箱、7200 s 为 7 箱、10800 s 为 7 箱）；其余货箱的期望时间只影响加权延误。

只读输入为题目 DOCX、运输机型与实体/电池表、逐箱清单、节点表及 DEM；已有 240 条有向航段 DEM 最高高程缓存可复用，但要按问题一参考口径重新使用节点表海拔和 WGS84 测地距离，以保持问题一、二物理规则一致。结果至少包含逐架次路线、机型、无人机、电池、开始与返回时刻、能耗、SOC；逐箱所属架次及交付完成时刻；无人机与电池占用/充电区间；硬时限、及时性、任务完成时间、总能耗和架次数的比较。

| 合同项 | 问题二定义 |
|---|---|
| 结论类型 | 一份满足所有硬时限和资源约束的可执行运输计划，逐箱与逐架次结果及目标权衡 |
| 目标优先 | 先消除硬时限违反，再降低应急优先系数加权延误 $F_1$，随后比较最晚返回时刻 $F_2$、总能耗 $F_3$、架次数 $F_4$；权衡方案单独报告非支配点 |
| 物理约束 | 每箱恰好配送一次；任一航段载荷为尚未卸下的货箱质量；起飞载重/体积不超机型上限；全架次能耗不超满电可用能量乘 $1-\rho_g$ |
| 时序约束 | 架次开始到返回连续占用一架同型无人机和一组同型电池；返回后电池须按两阶段模型充至满电才可再次投入；同一资源占用区间不得重叠 |
| 输出核验 | 独立从逐箱、航段、机型表重算每段载荷、时间、能耗、SOC、交付时刻、资源区间和目标值 |

## 统一多点航段模型

对序列 $O01\to i_1\to\cdots\to i_r\to O01$，每条有向航段沿用题目附录 2 和问题一参考口径：WGS84 水平距离 $d_{ij}$，巡航海拔 $H_{ij}$ 为沿线 DEM 最高高程加 50 m，起终点作业海拔分别为节点表地面高度及服务区地面高度加 30 m。爬升高度、下降高度由各自端点与本航段巡航海拔差计算。离开第 $j$ 个服务区后，已交付货箱质量从后续有效载荷中扣除。每段能耗为

$$e_{ijg}(q)=E_g^{\mathrm{use}}\frac{d_{ij}}{L_g(q)}+\frac{(m_g^0+q)9.81h_{ij}^{\uparrow}}{3.6\times10^6\eta_g},\qquad L_g(q)=L_g^0-(L_g^0-L_g^F)(q/Q_g)^{3/2}.$$

每段时间为 $h_{ij}^{\uparrow}/v_g^{\uparrow}+d_{ij}/v_g^{\mathrm c}+h_{ij}^{\downarrow}/v_g^{\downarrow}$。架次开始时先经历固定准备和全部货箱装载时间；到达站点后经历一次基础交接和该站每箱附加交接时间，该站全部货箱在交接完成时记为送达；最后返回 O01。以总能耗 $e_s$ 判定 $1-e_s/E_g^{\mathrm{use}}\ge\rho_g$。这是与参考 PDF 一致的明确时间解释，并对每个实际箱子的硬时限逐一核验。

## 资源解码与联合优化

候选解由一组有序运输架次组成，每架次携带具体箱号、机型和访问顺序；解码器在对应机型的所有实体无人机—共享电池组合中选择最早可同时开始的组合。用 $u$ 表示实体无人机，$c$ 表示电池，$b$ 专指货箱。架次 $s$ 的开始时刻为 $\max\{a_u,a_c\}$，返回后无人机可再次执行任务；电池充满时刻为返回时刻加 $C_g(\sigma_s)$。当返航 SOC $\sigma<0.9$，$C_g(\sigma)=T_g[0.65(0.9-\sigma)/0.9+0.35]$；否则 $C_g(\sigma)=T_g[0.35(1-\sigma)/0.1]$。电池不跨机型共享，所有电池初始 SOC 为 1。解码器产生逐箱交付时刻 $t_b$，硬时限 $h_b$ 的箱要求 $t_b\le h_b$。

优化采用问题特定的大邻域搜索：先按硬时限与期望时间构造可行初始架次，再通过移除—重插、同站合并、跨站插入、站序交换、机型变更和架次重排改进。每次变动均由精确航段评估与资源解码器验证。主方案在硬约束可行集内按 $(F_1,F_2,F_3,F_4)$ 词典序比较；多目标权衡另外改变优先级或加权目标，并保留非支配方案。这是启发式可行解搜索，不把参考 PDF 的“数量级加权”称为严格词典序，也不宣称未有下界证明的全局最优。ALNS 的移除和重插框架可追溯至 Ropke 与 Pisinger (2006), *Transportation Science*, DOI [10.1287/trsc.1050.0135](https://doi.org/10.1287/trsc.1050.0135)；本题的载荷能耗及电池充电约束来自题面，不能直接照搬文献实例。

令 $w_b$ 为应急优先系数、$d_b$ 为期望时间，及时性指标 $F_1=\sum_b w_b\max(0,t_b-d_b)$，单位为“权重秒”；完成时间 $F_2=\max_s t_s^{\mathrm{return}}$，单位 s；总能耗 $F_3=\sum_s e_s$，单位 kWh；架次数 $F_4=|\mathcal S|$。硬时限为独立可行性门禁，不用有限大罚项取代。优化过程保存已验证的最好可行解及随机种子、迭代记录，时间/能耗/架次权衡仅对有可复核交付表的方案作结论。

## 假设与失效条件

1. **关键**：每箱不可拆、只在其所属服务区卸货且仅配送一次；若数据改变必须重新构造全部架次。
2. **关键**：实体无人机和电池均从 O01 满电开始，一架次占用从开始到返回，电池返回后充至 100% 才可再出发；若允许途中换电、充电或其他基地，解码器失效。
3. **关键**：医疗期望时间与首批截止时间作为硬约束；若官方另有时间判定，应重新运行时限检查与搜索。
4. **可放宽**：为控制搜索规模，初版插入搜索以最多 3 个站点为候选；题面未限制站数，因此它是启发式搜索范围而不是原问题的可行域证明。必要时放宽并测试 4 站及以上改善可能。
5. **可放宽**：DEM 最高高程沿用已生成的 240 航段缓存，未逐条按参考 PDF 的 0.05 像元重采样；若对几何精度提出更严要求，重新采样并复算全部结果。
6. **可放宽**：用列表解码器选择当前最早可用资源，给出资源可行时刻；这不保证固定架次集合下的全局最短完成时间，应通过重排及独立局部精化检验。

## 实现与验证计划

先产生 240 条与问题一一致的有向航段，并用 S008 自检数值。对单架次多点能量、逐站送达和两阶段充电做最小纵向切片，交由 P1 独立核验后开展全量求解。以参考 PDF 所列的可行/不可行多点线路为物理回归点，但独立从本仓库原始工作簿与几何缓存计算。逐架次结果应写入官方模板 `Q2_运输架次`，逐箱结果写入 `Q2_逐箱交付`，其他工作表保持原值。全量后独立审计 80 箱唯一覆盖、资源区间、硬时限与指标汇总；对多个种子、权重/优先级与重排策略给出非支配分析，而不把单次搜索的缺口解释为模型定理。原始数据图、优化过程图、结果图和流程图按编程阶段门禁生成。
